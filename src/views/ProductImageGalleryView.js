let ProductImageGalleryView = Backbone.View.extend({

	init: function() {
		// this.el = document.getElementById( 'product_gallery' );
		// console.log( this.el );
		// this.$el = $( this.el );

		// this.delegateEvents({
		// 	'click': 'handleClick'
		// });
	},

	handleClick: function( event ) {

	}

});

export default ProductImageGalleryView;